*.iml
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies

android/local.properties
android/key.properties
android/.gradle/
android/gradlew.bat
android/**/*.keystore
android/**/*.jks

*.g.dart

# Windows desktop build artifacts (regenerated by `flutter pub get` / build;
# contains engine binaries that shouldn't be committed).
windows/flutter/ephemeral/

# Inno Setup output (generated installer .exe)
Output/
