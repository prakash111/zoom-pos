workflows:
  android-debug:
    name: Zoom POS Mobile - Debug Build
    max_build_duration: 30
    instance_type: mac_mini_m2

    environment:
      flutter: stable
      java: 17

    triggering:
      events:
        - push
      branch_patterns:
        - pattern: "*"
          include: true

    scripts:
      - name: Get Flutter packages
        script: |
          flutter pub get

      - name: Build debug APK
        script: |
          flutter build apk --debug --android-skip-build-dependency-validation

    artifacts:
      - build/app/outputs/flutter-apk/*.apk

  android-release:
    name: Zoom POS Mobile - Release Build (signed)
    max_build_duration: 30
    instance_type: mac_mini_m2

    environment:
      flutter: stable
      java: 17
      vars:
        # Keystore committed at zoom-pos.jks (PKCS12, single entry).
        KEYSTORE_FILE: "zoom-pos.jks"
        KEYSTORE_PASSWORD: "123456789"
        KEY_ALIAS: "zoom-pos"
        KEY_PASSWORD: "123456789"

    triggering:
      events:
        - push
      tag_patterns:
        - pattern: "mobile-v*"
          include: true

    scripts:
      - name: Get Flutter packages
        script: |
          flutter pub get

      - name: Set up release keystore
        script: |
          KEYSTORE_PATH="$CM_BUILD_DIR/$KEYSTORE_FILE"
          if [ ! -f "$KEYSTORE_PATH" ]; then
            echo "Keystore not found: $KEYSTORE_PATH"
            exit 1
          fi
          cat > "$CM_BUILD_DIR/android/key.properties" <<EOF
          storePassword=$KEYSTORE_PASSWORD
          keyPassword=$KEY_PASSWORD
          keyAlias=$KEY_ALIAS
          storeFile=$KEYSTORE_PATH
          EOF

      - name: Build signed release APK
        script: |
          flutter build apk --release --android-skip-build-dependency-validation

      - name: Build signed release App Bundle
        script: |
          flutter build appbundle --release --android-skip-build-dependency-validation

    artifacts:
      - build/app/outputs/flutter-apk/*.apk
      - build/app/outputs/bundle/**/*.aab
      - build/app/outputs/**/mapping.txt
